import { component$ } from '@builder.io/qwik'
import type { GameSpecificForm } from '~/types'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default component$((_props: GameSpecificForm) => {
  return null
})
