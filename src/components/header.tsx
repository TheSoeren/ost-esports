import { component$ } from '@builder.io/qwik'

export default component$(() => {
  return (
    <header>
      <img
        src="/logo_wide.png"
        class="p-8 w-[30rem] mx-auto"
        width="1780"
        height="733"
      />
    </header>
  )
})
